# BOBR Adventure — Consolidated Triage (2026-06-11 exhaustive playtest)

**Source:** 5 parallel headless playtest agents (charcreate, ch1flow, criticals, lategame, layout) + Phase 2 booking agent (2026-06-10). Full verdicts in MB `bobr_game_testing/playtest2_*_20260611`. Screenshots under `/tmp/bobr-playtest2/`.
**Branch tested:** `feat/direct-booking-phase0-1-20260603` (uncommitted) on dev :3000.
**Note:** the previously referenced `TRIAGE_20260610.md` does not exist anywhere on disk — this document replaces it.

## The stall-modal mystery — SOLVED
The "full-screen modal that blocked everything" is the **AdventureRewardTracker tier-celebration modal** (`fixed inset-0 bg-black/75 z-[100]`), auto-fired at the Welcome tier (~5 min play or 1–2 locations), **stacking over any open modal** (verified blocking the Cloud Save dialog). Secondary blocker: **NarratorToast** sits over the COMPLETE CHAPTER button. The Next.js error overlay never appeared in any of the 5 sessions, even under deliberate store corruption.

## Branch verdict vs. live
- **Text sweep (206→0 sub-10px): VERIFIED SAFE** — zero layout/overflow regressions at 390px + 1440px across 11 surfaces.
- **Phase 2 booking: GREEN** — e2e inquiry→quote→deposit works, all APIs fail closed, no regression (2026-06-10 report).
- **4 criticals:** C1 save cross-load PASS · C4 client-mint PASS · C2 SPLIT (`/adventure/play` rehydrates; `/adventure/character` fix reads wrong key) · C3 SPLIT (`/game` guarded; ExplorationMap WASD unguarded).
- **One fix needs a follow-up:** the confrontation impure-updater fix uses captured HP values → lost updates under rapid clicks (was: double-fire side effects; now narrower but real).
- Everything else found is **pre-existing on live today**.

## P0 — CRITICAL (game-promise breaking, all pre-existing)
| ID | Bug | Where |
|---|---|---|
| P0-1 | **Cynthia ending unreachable** — `ClueGameUnlock.tsx` imported nowhere; ch5 completion just `router.push('/game')`; `/clue-game` stays locked after full completion. The finish-game→Cynthia→QR-hunt chain is dead | `play/page.tsx:1053-1057` |
| P0-2 | **Progression never persisted** — `addExperience`/`modifyStat`/level-ups never write `bobr_ot_character`; Level 3→1 on reload, ~245 XP lost | `oregon-trail/characterContext.tsx` |
| P0-3 | **Karma wallet never persists in adventure** (gated on OT-only `walletMode`); balance resets to 400 while purchases persist → **spends are free after reload**; no balance shown in UI | `saveLoadContext` / wallet init |

## P1 — HIGH
| ID | Bug | Where |
|---|---|---|
| P1-1 | Reward modal auto-pops **over open modals**, blocking buttons (the stall); mobile tracker panel (320×464, no collapse) covers ← MAP button — taps fail | `AdventureRewardTracker.tsx:140` |
| P1-2 | Rapid-click ATTACK = N attacks/turn (stale phase guard) + **lost HP updates** (captured values; 18 dmg logged → 2 applied) + DEFEND +4 AC never applies (stale closure). Combat needs reducer/functional updates | `ConfrontationView.tsx:152-212` |
| P1-3 | Camp rapid-click: N× rewards, **negative days** ("-5 DAYS LEFT") | `CampManagement.tsx:43-70` |
| P1-4 | `/adventure/character` "No Character Found" with live char — fix reads `bobr_rpg_session`, data in `bobr_ot_character`; same wrong key kills "Continue Adventure" on `/adventure` title | `character/page.tsx`, `adventure/page.tsx:198` |
| P1-5 | New game **silently destroys** existing save (single implicit slot, no warning) | save flow |
| P1-6 | ExplorationMap WASD handler swallows typing in focused inputs (passphrase "wasd 42" → " 42") | `ExplorationMap.tsx:498-503` |
| P1-7 | **3 of 9 ch1 locations unreachable** (Sacramento trio one-way into pawnee_camp); their clues/NPCs unobtainable; every reachable location becomes mandatory | `chapterLocations.ts` connectedTo |
| P1-8 | Skill-tree stat bonuses never apply (`getSkillTreeBonuses` imported, never called) | `play/page.tsx:47` |
| P1-9 | Character-creation picks wiped by ANY back-nav incl. Review "← CHANGE PICKS" (local state unmounts) → trait-less character, 12 picks burned | `ZeroSumPicker.tsx:39` |

## P2 — MEDIUM
- `requiresBackground` never enforced (Doctor took Badge of Authority) — `ZeroSumPicker.tsx`
- Discovered counter counts all-chapter ids vs current total ("30 / 10 discovered")
- Narrator toast clipped 16px off-screen mobile (`w-full mx-4` vs translate-center) — `play/page.tsx:583`
- Duplicate zombie Pixi canvas (Strict-Mode double-init race) — `ExplorationMap.tsx` destroy guard
- Invisible XP: NPC/encounter XP → character XP, sidebar shows totalXP (toast says +10, number unmoved)
- Infinite grind: TEND ANIMALS/REST/SEARCH/NPC-retalk repeatable, no cooldowns
- Alignment immutable in adventure (karmaReward.lawful dropped; "True Neutral 1.0x" forever)

## P3 — LOW / cleanup
- **Security:** 6 plaintext easter-egg discount codes (10–50%) in client bundle — `oregon-trail/data/discountEngine.ts:492-540` (dead data; delete or move server-side)
- CSP console flood: `:8131/health` (164×/session) + `:8131/karma/transfer` + `:11434/api/tags` blocked by `connect-src 'self' ws:` — karma sync can never work from the page
- Dead code: `QuestLog.tsx`, `DialogueView.tsx`, `data/quests.ts`, `data/dialogues.ts` (adventure) — unmounted everywhere
- "-1 min" tracker on first paint (playStartTime not persisted); char name never on HUD; disabled picks show no reason; "+20 Health" grants +1 DUR; NPCs speak third-person dialogueHint verbatim; ally "2 ch. left" static; sidebar omits ally bonus
- Sitewide `PixelNavigation.tsx` still 8px (outside sweep scope); canvas/SVG map labels ~7-8px drawn
- **Live-site:** Matterport 3D tour on home page is dead (model 404 on Matterport's servers) — independent of branch
- `/api/saves` 404 surfaces as raw console error for new players

## Verified-correct (for the record)
Pick economy exact (25/25 toggles), stat assembly creation→HUD→storage exact, clue XP ledger exact, faction rep arithmetic, camp day ledger, chapter gate 6/9, ch1→ch2 carry, confrontation outcome idempotency (endedRef), all 7 fight resolutions resume travel (no stranding), milestone dedup, corrupt-save graceful recovery, save roundtrip 12/12 fields, no NaN/softlock anywhere, zero uncaught exceptions across all 5 sessions.

## Recommended order of work
1. Commit the branch (it is strictly better than live; all P0s pre-exist on live).
2. Quick wins on the branch family: P1-2 functional updates + phase/processing guards (P1-3 same class), P1-4 storage-key fix, P1-6 input guard, P1-1 modal stacking guard.
3. P0-1 mount Cynthia in ch5 completion; P0-2/P0-3 persistence (save character on change; init wallet in adventure).
4. P1-7 location graph fix; P1-8 call the bonuses fn; P1-9 lift picks state.
5. P2/P3 sweep incl. easter-egg code removal + CSP/karma-sync decision.
